import { ValidationForId } from "./IdQuery.validation";
import { ValidationForLoginRequest } from "./login.validation";
import { ValidationForPagination } from "./pagination.validation";
import { ValidationForRegistration } from "./registration.validation";

export {
    ValidationForLoginRequest,
    ValidationForPagination,
    ValidationForId,
    ValidationForRegistration
}